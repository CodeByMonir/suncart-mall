import React from 'react';

const page = () => {
    return (
        <div>
            <h1>Login Page</h1>
            this is the login page.
            login to access the dashboard.
        </div>
    );
};

export default page;